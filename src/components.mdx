import { Button, Card, Input, Select, Textarea, Badge, Avatar, Tooltip, Alert, ProgressBar, CodePreview, Accordion, AccordionItem, Checkbox, Modal, Radio, DefaultNavbar, TransparentNavbar, GlassNavbar, DarkNavbar, PrimaryNavbar, DefaultFooter, DarkFooter, GradientFooter, MinimalFooter, GlassFooter, ModernLanding, GlassLanding, HeroLanding, MinimalLanding, CreativeLanding } from '@/components';
import { useState } from 'react';
import { ModalPreview } from '@/components/previews/ModalPreview';
import { ButtonPreview } from '@/components/previews/ButtonPreview';
import { CardPreview } from '@/components/previews/CardPreview';
import { RadioPreview } from '@/components/previews/RadioPreview';
import { CheckboxPreview } from '@/components/previews/CheckboxPreview';

# Components

Welcome to the Layrix UI Components documentation! Here you'll find all our available components and how to use them.

## Button

A versatile button component with multiple variants and sizes.

<CodePreview
  code={`// Primary button
<Button variant="primary">Primary</Button>`}
  preview={<ButtonPreview />}
/>

## Card

Flexible container to display content with images, hover effects, and actions.

<CodePreview
  code={`<Card
  image="https://source.unsplash.com/random/800x600"
  title="Modern Card"
  description="A beautiful card with animations"
  hoverEffect="scale"
  button={{ text: 'Learn More', variant: 'primary', onClick: () => {} }}
/>`}
  preview={<CardPreview />}
/>

## Modal

Overlay dialog for user interactions.

<CodePreview
  code={`const [isOpen, setIsOpen] = useState(false);
<Modal isOpen={isOpen} onClose={() => setIsOpen(false)} />`}
  preview={<ModalPreview />}
/>

## Input

Single-line text input.

<CodePreview
  code={`<Input placeholder="Enter text..." />`}
  preview={<Input placeholder="Enter text..." />}
/>

## Select

Dropdown menu for options.

<CodePreview
  code={`<Select options={['One', 'Two']} />`}
  preview={<Select options={['One', 'Two']} />}
/>

## Checkbox

Toggle boolean values.

<CodePreview
  code={`<Checkbox label="Accept terms" />`}
  preview={<CheckboxPreview />}
/>

## Radio

Select one from many options.

<CodePreview
  code={`<Radio label="Option" />`}
  preview={<RadioPreview />}
/>

## Textarea

Multi-line text input.

<CodePreview
  code={`<Textarea placeholder="Enter message..." />`}
  preview={<Textarea placeholder="Enter message..." />}
/>

## Badge

Label or status indicator.

<CodePreview
  code={`<Badge variant="primary">New</Badge>`}
  preview={<Badge variant="primary">New</Badge>}
/>

## Avatar

Display profile image.

<CodePreview
  code={`<Avatar src="/avatar.jpg" alt="User" />`}
  preview={<Avatar src="/avatar.jpg" alt="User" />}
/>

## Tooltip

Text displayed on hover.

<CodePreview
  code={`<Tooltip content="Info"><Button>Hover</Button></Tooltip>`}
  preview={<Tooltip content="Info"><Button>Hover</Button></Tooltip>}
/>

## Alert

Important messages or notifications.

<CodePreview
  code={`<Alert variant="info">Info Alert</Alert>`}
  preview={<Alert variant="info">Info Alert</Alert>}
/>

## ProgressBar

Display completion progress.

<CodePreview
  code={`<ProgressBar value={50} />`}
  preview={<ProgressBar value={50} />}
/>

## Accordion

Expandable content sections.

<CodePreview
  code={`const accordionCode = (
    <Accordion>
      <AccordionItem title="Item 1">Content</AccordionItem>
    </Accordion>
  );

  const accordionPreview = (
    <div>
      <Accordion>
        <AccordionItem title="Item 1">Content</AccordionItem>
      </Accordion>
    </div>
  );

  {accordionCode}
  preview={accordionPreview}
/>`}
  preview={<div><Accordion>
    <AccordionItem title="Item 1">Content</AccordionItem>
  </Accordion></div>}
/>

## Navbar

Different navbar styles with customizable props.

<CodePreview
  code={`<DefaultNavbar links={[{ href: '#features', label: 'Features' }, { href: '#components', label: 'Components' }]} />`}
  preview={<div className="relative mt-4"><DefaultNavbar links={[{ href: '#features', label: 'Features' }, { href: '#components', label: 'Components' }]} /></div>}
/>

<CodePreview
  code={`<TransparentNavbar title="Layrix UI" />`}
  preview={<div className="relative mt-4"><TransparentNavbar title="Layrix UI" /></div>}
/>

<CodePreview
  code={`<GlassNavbar className="bg-white/30" />`}
  preview={<div className="relative mt-4"><GlassNavbar className="bg-white/30" /></div>}
/>

<CodePreview
  code={`<DarkNavbar links={[{ href: '#docs', label: 'Documentation' }, { href: '#api', label: 'API' }]} />`}
  preview={<div className="relative mt-4"><DarkNavbar links={[{ href: '#docs', label: 'Documentation' }, { href: '#api', label: 'API' }]} /></div>}
/>

<CodePreview
  code={`<PrimaryNavbar title="Layrix UI" className="bg-primary" />`}
  preview={<div className="relative mt-4"><PrimaryNavbar title="Layrix UI" className="bg-primary" /></div>}
/>

## Landing Pages

Modern and attractive landing page variants.

<CodePreview
  code={`<ModernLanding>
  <h1 className="text-4xl font-bold mb-4">Welcome to Layrix UI</h1>
  <p className="text-lg text-gray-600">A modern UI component library built with React and Tailwind CSS</p>
</ModernLanding>`}
  preview={<div className="relative mt-4"><ModernLanding>
    <h1 className="text-4xl font-bold mb-4">Welcome to Layrix UI</h1>
    <p className="text-lg text-gray-600">A modern UI component library built with React and Tailwind CSS</p>
  </ModernLanding></div>}
/>

<CodePreview
  code={`<GlassLanding>
  <h1 className="text-4xl font-bold mb-4">Glass Effect Landing</h1>
  <p className="text-lg text-gray-600">A modern UI component library built with React and Tailwind CSS</p>
</GlassLanding>`}
  preview={<div className="relative mt-4"><GlassLanding>
    <h1 className="text-4xl font-bold mb-4">Glass Effect Landing</h1>
    <p className="text-lg text-gray-600">A modern UI component library built with React and Tailwind CSS</p>
  </GlassLanding></div>}
/>

<CodePreview
  code={`<HeroLanding>
  <h1 className="text-4xl font-bold mb-4">Hero Landing</h1>
  <p className="text-lg">A modern UI component library built with React and Tailwind CSS</p>
</HeroLanding>`}
  preview={<div className="relative mt-4"><HeroLanding>
    <h1 className="text-4xl font-bold mb-4">Hero Landing</h1>
    <p className="text-lg">A modern UI component library built with React and Tailwind CSS</p>
  </HeroLanding></div>}
/>

<CodePreview
  code={`<MinimalLanding>
  <h1 className="text-4xl font-bold mb-4">Minimal Landing</h1>
  <p className="text-lg text-gray-600">A modern UI component library built with React and Tailwind CSS</p>
</MinimalLanding>`}
  preview={<div className="relative mt-4"><MinimalLanding>
    <h1 className="text-4xl font-bold mb-4">Minimal Landing</h1>
    <p className="text-lg text-gray-600">A modern UI component library built with React and Tailwind CSS</p>
  </MinimalLanding></div>}
/>

<CodePreview
  code={`<CreativeLanding>
  <h1 className="text-4xl font-bold mb-4">Creative Landing</h1>
  <p className="text-lg">A modern UI component library built with React and Tailwind CSS</p>
</CreativeLanding>`}
  preview={<div className="relative mt-4"><CreativeLanding>
    <h1 className="text-4xl font-bold mb-4">Creative Landing</h1>
    <p className="text-lg">A modern UI component library built with React and Tailwind CSS</p>
  </CreativeLanding></div>}
/>

## Footer

Different footer styles with customizable content.

<CodePreview
  code={`<DefaultFooter><p>© 2025 Layrix UI. All rights reserved.</p></DefaultFooter>`}
  preview={<div className="relative mt-4"><DefaultFooter><p>© 2025 Layrix UI. All rights reserved.</p></DefaultFooter></div>}
/>

<CodePreview
  code={`<DarkFooter><p>© 2025 Layrix UI - Built with 💙</p></DarkFooter>`}
  preview={<div className="relative mt-4"><DarkFooter><p>© 2025 Layrix UI - Built with 💙</p></DarkFooter></div>}
/>

<CodePreview
  code={`<GradientFooter><p>© 2025 Layrix UI - Powered by creativity ✨</p></GradientFooter>`}
  preview={<div className="relative mt-4"><GradientFooter><p>© 2025 Layrix UI - Powered by creativity ✨</p></GradientFooter></div>}
/>

<CodePreview
  code={`<MinimalFooter><p>© 2025 Layrix. Simple. Clean. Elegant.</p></MinimalFooter>`}
  preview={<div className="relative mt-4"><MinimalFooter><p>© 2025 Layrix. Simple. Clean. Elegant.</p></MinimalFooter></div>}
/>

<CodePreview
  code={`<GlassFooter><p>© 2025 Layrix Glass Footer</p></GlassFooter>`}
  preview={<div className="relative mt-4"><GlassFooter><p>© 2025 Layrix Glass Footer</p></GlassFooter></div>}
/>
